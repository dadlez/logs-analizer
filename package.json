{
  "name": "logs-analyzer",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "ready": "vp fmt && vp lint && vp run test -r && vp run build -r",
    "dev": "vp run api#dev website#dev",
    "dev:api": "vp run api#dev",
    "dev:website": "vp run website#dev",
    "build:api": "vp run api#build",
    "build:website": "vp run website#build"
  },
  "devDependencies": {
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@10.32.1"
}
